\documentclass{article}

\usepackage[dblblindworkshop]{neurips_2026}
\workshoptitle{Who Verifies the Agents? Toward Reliable Agent Development}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{array}
\usepackage{booktabs}
\usepackage{float}
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage{microtype}
\usepackage{url}
\usepackage{xcolor}

\input{generated/paper_numbers.tex}

\title{The Wrong Memory at the Right Time:\\
Evaluating Retrieval Admissibility in Long-Term Agents}

\author{Anonymous Author(s)}

\begin{document}

\raggedbottom

\maketitle

\begin{abstract}
Long-term memory is usually evaluated by relevance, yet a relevant record may belong
to another principal, violate policy, or describe an incompatible state.  We
introduce \emph{retrieval admissibility}: a query-conditioned, three-valued target
with matched-recall risk bounds, record-level accounting through prompt exposure,
and answer-level disclosure.

Across \NaturalMemoryCount{} memories and \NaturalQueryCount{} queries from RHELM
and MemOps, trusted namespace support improves retrieval utility and risk together.
At top-20, recall rises from \TopTwentyGlobalRecall{} to
\TopTwentyNamespaceRecall{}, penalized admissibility upper risk falls from
\TopTwentyGlobalAdmRisk{} to \TopTwentyNamespaceAdmRisk{}, and exact-search
candidate work drops \CandidateWorkRatio{}$\times$.  On a frozen
\NaturalEndToEndCaseCount{}-case natural sample, the same intervention improves
judged answer accuracy by \DeepSeekNaturalAccuracyDelta{},
\GeminiNaturalAccuracyDelta{}, and \GPTLunaNaturalAccuracyDelta{} for three
separately reported readers; all intervals exclude zero, and over-refusal also falls.
Additional released-policy deletion lowers route risk without an established answer-
utility gain, while text-only verification increases route risk.

In paired exposure tests, four separately reported readers have positive selectivity
gaps, but DeepSeek V4 Pro retains a strictly positive relevant-inadmissible
disclosure effect.  Together, the results support enforcing reliable eligibility
constraints before prompt assembly in the tested settings, while showing that more
deletion is not monotonically better.  The accompanying anonymized artifact
regenerates every reported table and figure without provider calls.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

The hardest failures in long-term agent memory are not irrelevant memories.  They are
the right facts under the wrong conditions.  An earlier delivery address can answer
an address question, another user's market note can answer a market question, and a
superseded date can answer a date question.  Their semantic relevance is precisely
what makes them difficult to detect: a system may retrieve the needed fact and even
produce the correct answer while relying on evidence that should never have shaped
that answer.  Retrieval recall and final-answer accuracy alone cannot expose this
failure.

We study this gap through \emph{retrieval admissibility}: whether a memory that is
relevant to a query is also eligible under the current principal, purpose, intent,
and time.  Relevance asks whether a record can answer the question; admissibility asks
whether it may be used to answer it.  This decision belongs to the memory--query
context rather than to the record alone.  A superseded value may be inadmissible for
``what is true now?'' yet indispensable for ``how did it change?''  Likewise, a
topically matched record may remain unusable because it belongs to another principal,
violates an active policy, or was explicitly forgotten.

Figure~\ref{fig:pipeline} grounds this distinction with four audited examples.  A
market memory fails scope, an earlier Lisbon date fails lifecycle compatibility for
a current-state query, and a forgotten phone detail fails policy.  The historical
medication example provides the complementary case: an old state remains admissible
because the query explicitly asks for change over time.  These examples motivate a
query-conditioned evaluation target that preserves the reason a memory is admitted,
excluded, or left unresolved.

We operationalize admissibility with a three-valued target, utility-matched route
risk, record-level accounting through prompt exposure, and answer-level disclosure.
Missing governance evidence remains unresolved, and a route cannot appear safer
merely by returning less useful context.

Three separate analyses probe \emph{candidate support} under metadata corruption,
\emph{selective verification} of missing eligibility decisions, and \emph{prompt
exposure} to relevant inadmissible records.  Each uses its appropriate population so
candidate-set, verifier, and reader failures remain identifiable.

\begin{figure}[!t]
  \centering
  \includegraphics[width=\textwidth]{generated/verification_pipeline.pdf}
  \caption{\textbf{Retrieval admissibility and its enforcement boundary.}
  (a) Audited RHELM/MemOps examples show that topical relevance does not determine
  eligibility: wrong-principal, superseded, and explicitly forgotten records are
  excluded, while an old state is admitted for a history query.  (b) The framework
  records tri-valued relevance, scope, policy, and lifecycle judgments; evaluates
  routes at matched evidence recall; tracks memory IDs through prompt exposure; and
  separately records answer-level target disclosure.  The natural closure follows
  the same 1,523 cases from route to judged answer for each reader; the controlled
  exposure intervention remains a separate population, and reader estimates are
  never pooled.  Full examples appear in
  Appendix~\ref{app:verification-examples}.}
  \label{fig:pipeline}
\end{figure}

Table~\ref{tab:natural-end-to-end} closes the natural path from candidate support to
judged answer utility, while the remaining diagnostics explain why additional
filtering does not automatically help.  Our contributions are (i) a
three-valued admissibility target with matched-recall bounds, (ii) a stage-localized
protocol spanning natural retrieval, corruption, verification, and paired exposure,
and (iii) same-population natural evidence that reliable namespace constraints improve
both route quality and downstream answer utility, while stricter semantic filtering
is not a consistent substitute for pre-prompt admission.

\section{Related Work}

\paragraph{Long-term memory capability.}
LoCoMo, LongMemEval, MemBench, and MemoryAgentBench evaluate retention, retrieval,
reasoning, and capacity over long or multi-session histories
\citep{maharana2024locomo,wu2025longmemeval,tan2025membench,hu2026memoryagentbench}.  RHELM adds
heterogeneous temporal histories, and LongMemEval-V2 tests compact evidence gathering
from accumulated experience \citep{zhang2026rhelm,wu2026longmemevalv2}.

\paragraph{Validity, mutation, and contextual use.}
MemOps, Memora, STALE, A-TMA, and LoCoMo-Plus show that recall can conflict with
mutation, forgetting, outdated state, or latent constraints
\citep{hao2026memops,uddin2026memora,chao2026stale,shi2026atma,li2026locomoplus}.  We evaluate
this conflict per retrieved record and compare routes at matched evidence recall with
explicit bounds for unresolved labels.

\paragraph{Governed memory and pre-generation enforcement.}
GateMem, CIMemories, and CI-Work evaluate access, forgetting, and contextual
information flow;
Collaborative Memory, Governed Shared Memory, and RaMem implement permissions,
provenance, supersession, or validity-aware retrieval
\citep{ren2026gatemem,mireshghallah2026cimemories,fu2026ciwork,rezazadeh2025collaborative,
margalit2026governed,yang2026ramem}.  ABAC, Securing the Agent, and SD-RAG likewise
motivate authorization before generation \citep{hu2019abac,arceo2026securing,
almasoud2026sdrag}.  We supply a common measurement layer for these controls.

\paragraph{Stagewise analytical evaluation.}
HELM, AgentBoard, RAGChecker, and MEMPROBE motivate multi-metric, intermediate-stage
analysis \citep{liang2023helm,ma2024agentboard,ru2024ragchecker,ma2026memprobe}.
Our record-level estimands extend that view across retrieval, verification, exposure,
and disclosure without collapsing the path into final-answer accuracy.


\section{Retrieval Admissibility Evaluation Framework}
\label{sec:framework}

\subsection{Framework overview}

Given a query context $z$, a candidate memory $m$, and a retrieval arm $\alpha$,
the framework evaluates three linked objects: record-level eligibility,
utility-matched route risk, and disclosure after candidate exposure.  A route may use
symbolic metadata, a learned verifier, or no eligibility filter; the common audit
unit is the same memory ID through retrieval and prompt exposure; the answer trace
records only whether target content is disclosed.

\subsection{Query-conditioned admissibility}

Let $z=(q,p,\kappa,t)$ contain the query, authenticated principal, governing policy,
and evaluation time.  For memory $m$, the evaluator records relevance $r_z(m)$,
scope $n_z(m)$, policy permission $g_z(m)$, and lifecycle compatibility $\ell_z(m)$.
Each axis lies in $\mathcal{T}=\{1,0,?\}$: established positive, established
negative, or unresolved.  Here \emph{unresolved} names an evaluator state;
\emph{unknown} below is reserved for an explicit verifier abstention.

We combine three-valued decisions using
\begin{equation}
\bigwedge\nolimits_{3}(x_1,\ldots,x_d)=
\begin{cases}
0, & \exists j:x_j=0,\\
1, & \forall j:x_j=1,\\
?, & \text{otherwise}.
\end{cases}
\label{eq:three-valued}
\end{equation}
The admissibility and usability targets for a memory--context pair are
\begin{align}
a_z(m)
&=\bigwedge\nolimits_{3}
  \left(n_z(m),g_z(m),\ell_z(m)\right),
\label{eq:admissible}\\
u_z(m)
&=\bigwedge\nolimits_{3}
  \left(r_z(m),a_z(m)\right).
\label{eq:usable}
\end{align}

One established governance violation therefore excludes a candidate; usability also
requires relevance.  Unresolved evidence is never converted into an implicit pass or
denial.  Lifecycle remains relational: a superseded value may be excluded for
``what is current?'' and retained for ``how did it change?''  Appendix
\ref{app:metric-details} gives the released-field mapping.

\subsection{Stagewise record accounting}

The target $a_z(m)$ is distinct from a metadata rule, oracle, or verifier prediction
$\widehat a_{qm\alpha}$.  We retain memory identity across storage $S_m$, retrieval
$R_{qm\alpha}$, and reader-visible exposure $E_{qm\alpha}$, which obey
\begin{equation}
E_{qm\alpha}\leq R_{qm\alpha}\leq S_m.
\label{eq:stage-order}
\end{equation}
We also record answer disclosure $Y_{q\alpha}$.  Thus the trace localizes failures in
candidate support, verification, prompt assembly, or disclosure.  For example,
trusted namespace support ranks only
$\mathcal{C}^{N}_z=\{m\in\mathcal{M}:n_z(m)=1\}$; other arms substitute global,
governance-filtered, routed, or verifier-filtered support without changing the audit
unit.

\subsection{Utility-matched route risk}
\label{sec:matched-risk}

Let $\mathcal{G}_q$ be the known usable anchors: records whose relevance and all
three admissibility axes are established positive.  Any unresolved axis keeps a
record outside $\mathcal{G}_q$; a query with no such anchors is recall-unevaluable.
Let $\pi_{q\alpha}=(m_1,\ldots,m_n)$ be the ranking from arm $\alpha$.  At target
recall $\tau$, we score the smallest qualifying prefix:
\begin{align}
\operatorname{Rec}_{q\alpha}(k)
&=\frac{|\{m_1,\ldots,m_k\}\cap\mathcal{G}_q|}{|\mathcal{G}_q|},\\
k^*_{q\alpha}(\tau)
&=\min\{k:\operatorname{Rec}_{q\alpha}(k)\geq\tau\},\\
P_{q\alpha}(\tau)
&=\{m_1,\ldots,m_{k^*_{q\alpha}(\tau)}\}.
\label{eq:matched-prefix}
\end{align}
The route is feasible when
$F_{q\alpha}(\tau)=\mathbf{1}\{k^*_{q\alpha}(\tau)<\infty\}=1$.  Charging every item
before the same recall target prevents a route from appearing safer merely by
returning less useful evidence.

For a feasible prefix of size $N_{q\alpha}$ and status family $B\in\{U,A\}$, let
$c^B$, $v^B$, and $h^B$ count violations, positives, and unresolved labels.  We
report known-label risk, coverage, and partial-identification bounds
\citep{imbens2004confidence}:
\begin{align}
C^{K,B}_{q\alpha}
&=\frac{c^B}{c^B+v^B},\\
\gamma^B_{q\alpha}
&=\frac{c^B+v^B}{N_{q\alpha}},\\
\left[
C^{L,B}_{q\alpha},
C^{U,B}_{q\alpha}
\right]
&=\left[\frac{c^B}{N_{q\alpha}},
\frac{c^B+h^B}{N_{q\alpha}}\right].
\label{eq:matched-bounds}
\end{align}
The bounds are sharp under the recorded labels and have width $1-\gamma^B_{q\alpha}$.

To prevent infeasible routes from appearing artificially safe, we define the
penalized upper risk
\begin{equation}
\widetilde C^{U,B}_{q\alpha}
=
\begin{cases}
C^{U,B}_{q\alpha}, & F_{q\alpha}=1,\\
1, & F_{q\alpha}=0,
\end{cases}
\qquad
\operatorname{PCR}^{B}_{\alpha}
=
\left\langle
\widetilde C^{U,B}_{q\alpha}
\right\rangle_{\mathcal S},
\label{eq:penalized-risk}
\end{equation}
where $\langle\cdot\rangle_{\mathcal S}$ is instantiated in
Section~\ref{sec:evaluation-design}.  $B=U$ yields penalized non-usable upper risk;
$B=A$ yields penalized admissibility upper risk.  Thus $\operatorname{PCR}^{A}$ is
not a conditional contamination probability: it combines matched-prefix upper risk
with a unit penalty for infeasible queries.

\subsection{Exposure and disclosure}

For paired unit $i$ in relevance--admissibility cell $c$, let $D_i(1)$ and $D_i(0)$
denote literal target disclosure when a candidate is exposed and withheld.  We define
the cell effect and reader selectivity gap as
\begin{align}
\widehat{\Delta}_c
&=\frac{1}{n_c}\sum_{i\in c}\left[D_i(1)-D_i(0)\right],\\
\widehat S
&=\widehat{\Delta}_{\mathrm{rel,adm}}-
\widehat{\Delta}_{\mathrm{rel,inadm}}.
\label{eq:paired-exposure}
\end{align}
A positive $\widehat S$ indicates that exposure enables relevant admissible evidence
more than relevant inadmissible evidence; $\widehat{\Delta}_{\mathrm{rel,inadm}}$
must still be reported because a selective reader may disclose inadmissible evidence
after exposure.  The next section instantiates these record-, route-, and reader-level
quantities on separate natural and controlled populations.
\section{Experimental Design}
\label{sec:evaluation-design}

We organize the evaluation around the three questions in Section~1.  The support
question combines natural retrieval and metadata corruption; the verification
question combines natural and controlled diagnostics; and the enforcement question
uses paired exposure.  Table~\ref{tab:experiment-map} states the data, comparisons,
and outcomes for the five component analyses.  Natural, public-development, and
constructed populations are never pooled; the route-to-reader closure is an explicit
frozen subset of the natural population.

\begin{table}[!t]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.08}
  \caption{Main experimental setup.  Each row is a separate population; exact
  revision hashes and the complete parameter grid are retained in the appendix.}
  \label{tab:experiment-map}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.29\textwidth}
                       >{\raggedright\arraybackslash}p{0.37\textwidth}
                       >{\raggedright\arraybackslash}p{0.27\textwidth}@{}}
    \toprule
    Experiment and data & Systems compared & Primary outcomes \\
    \midrule
    \textbf{Natural support + closure.} Public RHELM + MemOps; \NaturalQueryCount{}
      retrieval queries and a frozen \NaturalEndToEndCaseCount{}-case reader sample
      & Nine retrieval arms; five closure routes for DeepSeek/Gemini and three
      primary routes for GPT Luna & Recall, admissibility risk, answer utility,
      over-refusal, typed disclosure \\
    \addlinespace[2pt]
    \textbf{Metadata reliability.} Same evaluation groups; 10 seeds per channel and
      eight corruption rates & Frozen namespace, policy, lifecycle, and combined
      filters under false allow, false deny, missing, and swap errors &
      Channel-specific weak-dominance brackets, typed violations, route work \\
    \addlinespace[2pt]
    \textbf{Natural verification.} 96 public-development queries; 24 calibration and
      72 analysis cases; fixed top-20 pools & Namespace dense, released-field oracle,
      and direct/abstaining text filters from GPT-5.6 Sol and Gemini 3.6 Flash & Route
      utility--risk and violation discrimination \\
    \addlinespace[2pt]
    \textbf{Controlled verification.} \ExposureScenarioCount{} scenarios; 32 focal
      and 64 stable pairs & Keep-all control, released-label oracle, GPT-5.6 Sol,
      Gemini 3.6 Flash, DeepSeek V4 Pro & Focal consistency, stable overflip, false
      denial/admission \\
    \addlinespace[2pt]
    \textbf{Paired exposure.} Same scenarios; \ExposurePairsPerReader{} pairs and
      384 requests per reader & One candidate withheld versus exposed for each of the
      three original readers; separate Claude Opus 5 replication & Cell-specific
      disclosure effect and selectivity gap \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Datasets and splits}

The natural retrieval audit combines public RHELM conversation histories and MemOps
profiles and operation traces \citep{zhang2026rhelm,hao2026memops}.  Development has
23 disjoint persona/profile groups, 45,978 memories, and 1,104 queries; evaluation has
\NaturalGroupCount{} groups, \NaturalMemoryCount{} memories, and
\NaturalQueryCount{} queries.  For RHELM, a query can retrieve only turns strictly
earlier than its date; for MemOps, every method-visible dialogue memory in the split
is eligible for global retrieval.  Namespace support then restricts candidates to the
query persona or profile.  Released evidence IDs, provenance, and deterministic
operation replay provide relevance anchors and governance states.  We use no
candidate subsampling, positive injection, or human-overlap label.

The text-verification diagnostic is a stable-hash sample of 96 public-development
queries: 32 RHELM current-state, 32 MemOps current-state, and 32 MemOps history
queries.  Eight cases per stratum select thresholds and 24 per stratum form the 72-case
analysis set.  A separate constructed diagnostic contains four scenarios for each of
principal scope, policy purpose, lifecycle intent, and as-of time.  Each scenario has
two paraphrased query pairs over a fixed three-candidate pool.  The paired exposure
experiment reuses those governing conditions with independently frozen relevance and
admissibility labels.  These diagnostic populations are not estimates of natural
prevalence.

The natural route-to-reader closure uses all 523 RHELM cases and an outcome-
independent simple random sample of 1,000/3,244 MemOps cases, frozen before reader
outcomes.  The resulting \NaturalEndToEndCaseCount{} cases span all 87 evaluation
groups and are paired across routes within each reader.  DeepSeek and Gemini cover
five routes; GPT Luna is a sequential robustness replication over the three primary
routes after a frozen continuation gate.  Reader estimates are never pooled.

\subsection{Models and comparison systems}

All dense retrieval arms use the pinned \texttt{Qwen/Qwen3-Embedding-8B} snapshot:
4,096-dimensional float32 vectors, a 512-token maximum, unit-normalized last-token
pooling, exact inner product, and stable memory-ID tie breaking.  The nine
development-selected arms are global BM25, global dense, BM25--dense reciprocal-rank
fusion, recency-augmented dense, namespace dense, query-agnostic current-only,
historical released-field v1, a threshold router, and a cluster router.  The primary
v2 support analysis holds the global- and namespace-dense rankings fixed across four
budgets; policy-only, lifecycle-only, and combined released-governance filters are
untuned attribution diagnostics.  Appendix~\ref{app:arms} gives the exact model
revision, scoring equations, and selected settings.

Natural text verification compares the fixed namespace-dense top-20 pool with a
released-field oracle, a thresholded text filter, and an abstaining variant using
complete outputs from \texttt{gpt-5.6-sol} and \texttt{gemini-3.6-flash}.  The
models receive only query text and visible time plus candidate key, rank, text, and
visible order.  Source and namespace IDs, required-evidence labels, released intent,
policy, lifecycle, distractor labels, embeddings, and scorer metadata remain hidden.
On the 24 calibration cases, model-specific violation thresholds---and the unknown
cutoff for the abstaining arm---are selected from frozen grids to maximize known-
violation recall subject to at most 1\% required-anchor false denial, followed by
precision, false-denial, threshold, and unknown-cutoff tie-breaks.  The selected
operating points are then frozen for the 72 analysis cases.  GPT-5.6 uses low effort
and Gemini uses low thinking for this diagnostic.  The direct and abstaining routes
select the same candidates at the resulting cutoffs; we retain both names to expose
the preregistered comparison rather than count them as independent evidence.

The controlled verifier and paired-exposure panels also include
\texttt{deepseek-v4-pro}.  Each model sees the same semantic task contract; provider
adapters do not change its meaning, and only complete bundles enter a comparison.
The paired-exposure configurations use reasoning \texttt{none} for GPT-5.6 Sol,
thinking \texttt{minimal} for Gemini 3.6 Flash, and disabled thinking for DeepSeek V4
Pro.  An independently executed fourth-reader replication uses
\texttt{claude-opus-5} with disabled thinking and medium effort.  It reuses the same
construction but is not pooled with or retroactively inserted into the original
three-reader execution.  Every reader contributes one accepted response per request,
with no judge, semantic repair, selective rerun, or cross-model pooling.

The natural closure uses \texttt{deepseek-v4-pro},
\texttt{gemini-3.6-flash}, and \texttt{gpt-5.6-luna} as separately reported
readers.  A blinded \texttt{claude-haiku-4-5-20251001} judge scores answer
correctness, quality, and over-refusal from the reference evidence; it does not see
the route or reader identity.  This shared judge makes the result cross-reader, not
cross-judge, evidence.  GPT Luna was run sequentially after the positive two-reader
continuation gate and is not an independently preregistered replication.

\subsection{Metrics}

Natural retrieval is macro-averaged within source and then equally across RHELM and
MemOps.  This instantiates $\langle\cdot\rangle_{\mathcal S}$ in
Equation~\ref{eq:penalized-risk} as the equal-source operator and fixes target recall
at $.8$.  We report evidence recall, feasible rate, and $\operatorname{PCR}^{A}$ as
primary retrieval outcomes; $B=U$ names the frozen historical selection family.
Typed scope, policy, and lifecycle violations are reported separately.

Candidate-level verifier diagnostics include balanced accuracy, Brier score,
calibration error, violation precision and recall, coverage, and required-anchor
false denial.  Controlled verification reports focal consistency and stable
overflip; paired exposure uses Equation~\ref{eq:paired-exposure}.  Natural downstream
outcomes are judged answer correctness, bounded answer quality, over-refusal, and
released-field protected/stale disclosure.  Answer and disclosure effects remain
reader-specific.  Appendix
\ref{app:metric-details} gives the exact definitions, undefined-denominator rules,
risk decomposition, and edge cases.

\subsection{Selection, uncertainty, and boundaries}

A 165-setting retrieval grid is selected on development only using a lexicographic
objective: maximize feasible rate, minimize historical v1 penalized non-usable upper
risk, then break ties by known-label risk, recall, work, and stable setting ID.  The
\NaturalRouteRows{} evaluation routes receive no retuning or repair.  Corrected v2
results are post-hoc re-scores of frozen rankings, and the released-governance v2
oracle receives no tuning.  Metadata corruption changes only method-visible fields;
labels, embeddings, dense scores, and retrieval settings remain fixed.  Each channel
uses nested rates $\{0,.02,.05,.10,.20,.30,.40,.50\}$ over ten seeds; support-expanding
errors rerank the resulting candidate set.  A dominance break is an adjacent observed
grid bracket, not an interpolated population threshold.

At each corruption rate, an arm \emph{weakly dominates} its reference when feasible
rate is no lower and penalized admissibility upper risk is no higher; equality is
allowed.  Namespace-corruption arms use global dense as their reference.  Policy,
lifecycle, and intent corruptions use clean namespace dense.  Recall, typed
violations, and candidates scored are diagnostics, not dominance criteria.

All reported intervals are pointwise 95\% percentile bootstrap intervals from
10,000 replicates; no simultaneous-coverage claim is made.  Fixed-budget natural
contrasts and released-field attribution resample namespace groups while preserving
equal-source macro-averaging.  Text verification resamples namespace groups
stratified by source and query intent.  Controlled verification and exposure
resample the 16 scenarios within governing-axis strata.  The natural closure uses
paired namespace-group resampling while preserving equal-source macro aggregation.
Corruption means are descriptive over ten seeds.  Models and distinct experimental
populations are never pooled, and none of these results is an official RHELM, MemOps,
or GateMem benchmark score.  Appendices~\ref{app:gatemem} and \ref{app:smoke} retain the secondary
observational GateMem audit \citep{ren2026gatemem} and implementation smoke.

\section{Results}

\begin{table}[!t]
  \centering
  \scriptsize
  \setlength{\tabcolsep}{3.2pt}
  \renewcommand{\arraystretch}{1.08}
  \caption{Natural same-population route-to-reader effects.  Entries are namespace
  dense minus global dense with pointwise 95\% paired namespace-group bootstrap
  intervals.  Readers are reported separately and share one blinded Claude Haiku
  judge.  $^{\dagger}$GPT-5.6 Luna is a sequential robustness replication.}
  \label{tab:natural-end-to-end}
  \input{generated/natural_end_to_end_results.tex}
\end{table}

\subsection{Namespace support improves downstream utility on the same natural cases}

The natural closure connects retrieval to judged answers on the same
\NaturalEndToEndCaseCount{} cases (Table~\ref{tab:natural-end-to-end}).  Namespace
dense raises route evidence recall by \NaturalEndToEndRecallDelta{} (95\% CI
\NaturalEndToEndRecallDeltaCI{}), feasible rate by
\NaturalEndToEndFeasibleDelta{} (\NaturalEndToEndFeasibleDeltaCI{}), and lowers
penalized admissibility upper risk by \NaturalEndToEndAdmRiskDelta{}
(\NaturalEndToEndAdmRiskDeltaCI{}).  Answer-accuracy, answer-quality, and over-
refusal intervals all favor namespace support for DeepSeek, Gemini, and GPT Luna,
with no pooling across readers.

More deletion is not monotonically better.  The released-policy gate lowers route
risk a further \NaturalPolicyAdmRiskDelta{} (\NaturalPolicyAdmRiskDeltaCI{}) relative
to namespace dense, but its answer-accuracy intervals include zero for all three
readers.  For DeepSeek and Gemini, the text-only verifier instead raises route risk
by \NaturalTextVerifierAdmRiskDelta{} (\NaturalTextVerifierAdmRiskDeltaCI{});
Gemini accuracy declines, while the DeepSeek interval includes zero.  Finally,
namespace effects on protected and stale disclosure include zero for every reader.
The same-population result therefore supports namespace-constrained candidate
support as a utility--risk intervention, not a general disclosure reduction or a
claim that stricter filtering always helps.

\begin{figure}[!t]
  \centering
  \includegraphics[width=\textwidth]{generated/constraint_reliability.pdf}
  \caption{Utility--risk effects of trusted metadata.  (a) Global-to-namespace
  movement at four fixed budgets.  (b) v2 policy/lifecycle decomposition;
  whiskers are pointwise 95\% namespace-group bootstrap intervals.  (c)
  Channel-specific weak-dominance brackets across nested corruption rates.  Namespace
  channels are compared with global dense; governance channels are compared with
  clean namespace dense.  Arrows mean no failure through .50, and summaries are
  descriptive means over ten seeds.}
  \label{fig:constraint-reliability}
\end{figure}

\subsection{Support constraints improve the utility--risk frontier}

The namespace advantage is already visible at top-20, not only top-100
(Figure~\ref{fig:constraint-reliability}a).  Recall rises from
\TopTwentyGlobalRecall{} to \TopTwentyNamespaceRecall{} (difference
\TopTwentyRecallDelta{}, 95\% CI \TopTwentyRecallDeltaCI{}), feasible rate from
\TopTwentyGlobalFeasible{} to \TopTwentyNamespaceFeasible{}
(\TopTwentyFeasibleDelta{}, \TopTwentyFeasibleDeltaCI{}), and penalized
admissibility upper risk falls from \TopTwentyGlobalAdmRisk{} to
\TopTwentyNamespaceAdmRisk{} (difference \TopTwentyAdmRiskDelta{},
\TopTwentyAdmRiskDeltaCI{}).  At top-100 the respective deltas are
\TopHundredRecallDelta{}, \TopHundredFeasibleDelta{}, and
\TopHundredAdmRiskDelta{}.  Every interval at all four tested budgets excludes zero.

Namespace support also reduces mean candidate scoring from
\GlobalDenseCandidates{} to \NamespaceDenseCandidates{} records per query, a
\CandidateWorkRatio{}$\times$ reduction.  This is a deterministic retrieval-work
diagnostic under exact search, not a wall-clock or production-latency claim.

The risk decomposition prevents a feasibility gain from masquerading as cleaner
feasible prefixes.  At top-100, the total \TopHundredAdmRiskDelta{} difference is the
sum of an infeasibility component \TopHundredInfeasibilityDelta{} and a
feasible-prefix admissibility component \TopHundredConditionalAdmDelta{}; both
intervals exclude zero.  These are post-hoc v2 scores on frozen rankings.  The
historical preregistered v1 result remains the penalized \emph{non-usable} upper risk
and is reported separately in Appendix~\ref{app:arms}.

\subsection{Metadata error direction determines whether constraints remain useful}

The v2 decomposition separates policy and lifecycle effects
(Figure~\ref{fig:constraint-reliability}b).  Policy-only filtering changes recall by
\PolicyOnlyRecallDelta{} (\PolicyOnlyRecallDeltaCI{}), feasible rate by
\PolicyOnlyFeasibleDelta{} (\PolicyOnlyFeasibleDeltaCI{}), and admissibility risk by
\PolicyOnlyAdmRiskDelta{} (\PolicyOnlyAdmRiskDeltaCI{}).  Lifecycle-only filtering
instead changes recall by \LifecycleOnlyRecallDelta{} and risk by
\LifecycleOnlyAdmRiskDelta{}: the evaluated two-intent rule removes useful evidence.
The combined v2 arm remains beneficial, but less so than policy alone.
Thus the incremental gain is policy-driven, not lifecycle-only headroom.

Corruption confirms that error direction matters, but only relative to each channel's
declared reference.  Against global dense, namespace false denial, missing labels,
and source-level label swaps remain weakly dominant through .10 and first fail at
.20.  At the swap failure point, recall is \NamespaceSwapTwentyRecall{} and feasible
rate is \NamespaceSwapTwentyFeasible{}.  Namespace false allow remains weakly
dominant through .50, but this aggregate verdict is not safety: at .50 its matched-
prefix wrong-scope exposure is \FalseAllowHalfWrongScope{} and it scores
\FalseAllowHalfCandidates{} candidates per query, versus roughly 1,551 when clean.
Against clean namespace dense, policy false denial and lifecycle false-stale fail at
the first nonzero .02 point, whereas policy false allow and fail-open missing pass
through .30 and first fail at .40.  These channel-specific observed brackets are not
universal metadata-accuracy thresholds.

\subsection{Text-only verifiers over-deny stable evidence on fixed candidates}

The released oracle preserves recall and feasibility while reducing admissibility
risk by \ReleasedOracleAdmRiskDelta{} (\ReleasedOracleAdmRiskDeltaCI{};
Appendix Figure~\ref{fig:inference-gap}a).  GPT-5.6 text filtering instead changes recall by
\OpenAIInferredRecallDelta{} (\OpenAIInferredRecallDeltaCI{}), feasible rate by
\OpenAIInferredFeasibleDelta{} (\OpenAIInferredFeasibleDeltaCI{}), and risk by
\OpenAIInferredAdmRiskDelta{} (\OpenAIInferredAdmRiskDeltaCI{}).  Gemini does not
realize the oracle benefit either: its recall, feasibility, and risk differences are
\GeminiInferredRecallDelta{}, \GeminiInferredFeasibleDelta{}, and
\GeminiInferredAdmRiskDelta{}.  The models occupy different classification operating
points (Appendix Table~\ref{tab:text-inference-diagnostics}), but neither converts
released-field headroom into a utility-preserving route.

Controlled pairs expose the mechanism (Appendix Figure~\ref{fig:inference-gap}b).  GPT-5.6
and Gemini classify all focal flips correctly, while DeepSeek reaches
\DeepSeekControlledFocalConsistency{}.  Yet stable-control overflip is
\GPTControlledOverflip{}, \GeminiControlledOverflip{}, and
\DeepSeekControlledOverflip{}, all above the frozen .05 ceiling.  Post-hoc errors are
dominated by false denial of stable-admissible memories
(\GPTControlledStableFalseDeny{}, \GeminiControlledStableFalseDeny{}, and
\DeepSeekControlledStableFalseDeny{}) rather than false admission of stable-
inadmissible controls.  Strong focal rule-following therefore does not establish
selective verification.

\begin{figure}[!t]
  \centering
  \includegraphics[width=\textwidth]{../results/counterfactual_exposure/figures/counterfactual_exposure.pdf}
  \caption{Controlled candidate exposure can enable literal-marker disclosure.  (a)
  Exposed-minus-withheld effects by relevance and admissibility.  (b) Reader-specific
  selectivity gaps and relevant-inadmissible effects.  Whiskers are pointwise 95\%
  intervals from 10,000 scenario-bootstrap replicates within governing-axis strata.
  Claude Opus 5 is a separate, unpooled replication; DeepSeek's positive interval is
  a prompt-level result, not a production-safety guarantee.}
  \label{fig:paired-exposure}
\end{figure}

\subsection{Constructed exposure reveals a reader-specific disclosure failure}

All four readers have positive selectivity-gap intervals
(Figure~\ref{fig:paired-exposure}).  DeepSeek alone has a strictly positive relevant-
inadmissible effect, \DeepSeekExposureInadmissible{}
(\DeepSeekExposureInadmissibleCI{}); the separate Opus replication has gap
\OpusExposureGap{} (\OpusExposureGapCI{}), but its inadmissible interval includes
zero.  Estimates remain reader-specific and identify constructed prompt-level
disclosure, not internal use or natural prevalence.

\section{Discussion}

Semantic ranking cannot repair misspecified support: authenticated principal and
provenance improve route quality and judged answer utility on the same natural cases.
Yet arbitrary deletion is not justified.  False denial removes required evidence,
the coarse lifecycle rule over-filters, and text-only verifiers reject stable
admissible candidates.  Systems should preserve unresolved status and enforce known
violations before prompt assembly, because one reader still disclosed relevant
inadmissible content after exposure.

\section{Limitations and Broader Impact}

The natural audit covers two public sources and released metadata; its reader closure
uses all RHELM but a frozen 1,000-case MemOps sample.  All readers share one blinded
judge, GPT Luna is sequential, and disclosure intervals include zero.  Thus utility
gains are neither cross-judge replication nor a general safety result.  V2 scores are
post-hoc, corruption brackets are not population thresholds, metadata rules are
approximations, and candidate counts are not production latency.  GateMem is
observational; verifier diagnostics contain 72 natural cases and 16 constructed
scenarios; paired exposure uses literal markers and measures neither internal use nor
natural prevalence.  Heterogeneous readers do not define a leaderboard.

\section{Conclusion}

Reliable namespace metadata improves route and answer utility on the same natural
cases, but stricter deletion is not monotonically beneficial and exposed
inadmissible records can still be disclosed.  Retrieval evaluation should therefore
measure admissibility at matched recall and enforce established violations before
prompt assembly.

\bibliographystyle{plainnat}
\bibliography{references}

\appendix

\section{Metric Details}
\label{app:metric-details}

For status family $B\in\{U,A\}$, the matched-prefix quantities in
Equation~\ref{eq:matched-bounds} obey
\begin{align}
C^{K,B}_{qa}&=\frac{c^B}{c^B+v^B}, &
\gamma^B_{qa}&=\frac{c^B+v^B}{N},\label{eq:known-coverage}\\
C^{L,B}_{qa}&=\frac{c^B}{N}=\gamma^B_{qa}C^{K,B}_{qa}, &
C^{U,B}_{qa}&=\frac{c^B+h^B}{N}
=\gamma^B_{qa}C^{K,B}_{qa}+(1-\gamma^B_{qa}).
\label{eq:partial-id}
\end{align}
Hence $C^{U,B}_{qa}-C^{L,B}_{qa}=1-\gamma^B_{qa}$.  The bounds are obtained by
assigning every unresolved item first to the positive and then to the negative class;
if every item is unresolved, $C^{K,B}$ is undefined but the interval remains $[0,1]$.

The aggregate in Equation~\ref{eq:penalized-risk} decomposes exactly within each
source.  Writing $\operatorname{PCR}^{B}_{as}$ for the source-$s$ term, we have
\begin{equation}
\operatorname{PCR}^{B}_{as}
=\underbrace{\frac{1}{|\mathcal{Q}^{+}_s|}\sum_{q\in\mathcal{Q}^{+}_s}(1-F_{qa})}_{\text{infeasibility}}
+\underbrace{\frac{1}{|\mathcal{Q}^{+}_s|}\sum_{q\in\mathcal{Q}^{+}_s}
F_{qa}C^{U,B}_{qa}}_{\text{feasible-prefix contribution}}.
\label{eq:risk-decomposition}
\end{equation}
The reported $\operatorname{PCR}^{B}_{a}$ is the equal-source mean of these terms.
Queries without known anchors are recall-unevaluable rather than assigned zero.

\paragraph{Retrieval and verifier diagnostics.}
Let $\mathcal Q_s^+$ be recall-evaluable queries from source $s$, with
$\mathcal S=\{\mathrm{RHELM},\mathrm{MemOps}\}$.  The equal-source macro operator,
evidence recall, feasibility, and typed violation rate are
\begin{align}
\langle X\rangle_{\mathcal S}
&=|\mathcal S|^{-1}\sum_{s\in\mathcal S}|\mathcal Q_s^+|^{-1}
  \sum_{q\in\mathcal Q_s^+}X_q,\\
\operatorname{Rec}_{qa}(k)
&=\frac{|\{m_1,\ldots,m_k\}\cap\mathcal G_q|}{|\mathcal G_q|},\qquad
F_{qa}=\mathbf 1\{\operatorname{Rec}_{qa}(n_{qa})\geq .8\},
\label{eq:retrieval-outcomes}\\
V^{(j)}_{qa}
&=|P_{qa}|^{-1}\sum_{m\in P_{qa}}\mathbf 1\{x_{zj}(m)=0\},\qquad
x_{zj}\in\{n_z,g_z,\ell_z\}.
\label{eq:typed-violation}
\end{align}
Among $M$ verifier candidates, let $\mathcal K$ index the $M_K$ known labels;
$y_i\in\{0,1\}$ and $p_i$ are the violation label and probability, and $I_b$ are
ten equal-width probability bins.  Indicators $d_i,k_i,v_i,r_i,c_i$ mark dropped,
known-label, known-violation, required-anchor, and covered status.  We use
\begin{align}
\operatorname{BA}
&=\tfrac12(\operatorname{TPR}+\operatorname{TNR}), &
\operatorname{Brier}
&=M_K^{-1}\sum_{i\in\mathcal K}(p_i-y_i)^2,\\
\operatorname{ECE}_{10}
&=\sum_b\frac{|I_b|}{M_K}|\bar y_b-\bar p_b|, &
\operatorname{Prec}_{V}
&=\frac{\sum_i d_iv_i}{\sum_i d_ik_i},
\label{eq:verification-calibration}\\
\operatorname{Rec}_{V}
&=\frac{\sum_i d_iv_i}{\sum_i v_i}, &
\operatorname{Cov}
&=M^{-1}\sum_i c_i,\qquad
\operatorname{FD}=\frac{\sum_i d_ir_i}{\sum_i r_i}.
\label{eq:filter-diagnostics}
\end{align}
For $J$ controlled focal pairs, let $\hat a^f_{\nu,+/-}$ be the focal argmax and
$\mathcal H_\nu$ its stable controls.  Focal consistency and stable overflip are
\begin{align}
\operatorname{FC}
&=J^{-1}\sum_{\nu=1}^{J}
  \mathbf 1\{\hat a^f_{\nu,+}=1\land\hat a^f_{\nu,-}=0\},\\
\operatorname{OF}
&=\frac{\sum_{\nu=1}^{J}\sum_{h\in\mathcal H_\nu}
  \mathbf 1\{\hat a^h_{\nu,+}\neq\hat a^h_{\nu,-}\}}
  {\sum_{\nu=1}^{J}|\mathcal H_\nu|}.
\label{eq:controlled-verification}
\end{align}
Unknown is a distinct argmax state; undefined denominators remain unavailable.

For the observational GateMem audit, let $X_{qa}$ indicate target exposure and
$Y_{qa}$ target disclosure.  For each checkpoint, form ordered discordant pairs
$D_q=\{(a,b):X_{qa}=1,\ X_{qb}=0\}$ and average first within checkpoint, then across
checkpoints:
\begin{equation}
d_q=\frac{1}{|D_q|}\sum_{(a,b)\in D_q}(Y_{qa}-Y_{qb}),\qquad
\widehat{\mathrm{RD}}=\frac{1}{|\mathcal{Q}_D|}
\sum_{q\in\mathcal{Q}_D}d_q.
\label{eq:discordant-rd}
\end{equation}
This prevents checkpoints with more route pairs from receiving more weight; it does
not make the route contrast causal.

For completeness, the released-field lifecycle rule used in Section~3.1 is
\begin{equation}
\ell_z(m)=
\begin{cases}
1, & s_m=\text{current},\\
1, & s_m\in\{\text{stale},\text{superseded}\},\ i_q=\text{history},\\
0, & s_m\in\{\text{stale},\text{superseded}\},\ i_q=\text{current},\\
?, & s_m=\text{unknown}\ \text{or}\ i_q=\text{unknown}.
\end{cases}
\label{eq:lifecycle}
\end{equation}
Writing $x_{zj}(m)$ for the recorded label on admissibility axis $j$, the verifier's
known exclusion reasons and trusted-namespace support are
\begin{align}
\operatorname{reason}_z(m)
&=\{j\in\{\mathrm{scope},\mathrm{policy},\mathrm{lifecycle}\}:x_{zj}(m)=0\},
\label{eq:reasons}\\
\mathcal{C}^{N}_z&=\{m\in\mathcal{M}:n_z(m)=1\},\qquad
\pi^{N}_z=\operatorname{Rank}(q;\mathcal{C}^{N}_z).
\notag
\end{align}
The typed violation metric is defined in Equation~\ref{eq:typed-violation}.
Finally, for leakage, utility, or refusal outcome $j$ and its own eligible population
$\mathcal{Q}_j$, the strict-minus-base contrast is
\begin{equation}
\Delta_j=\frac{1}{|\mathcal{Q}_j|}\sum_{q\in\mathcal{Q}_j}
\left(Y^{(j)}_{q,\mathrm{strict}}-Y^{(j)}_{q,\mathrm{base}}\right).
\label{eq:safety-utility}
\end{equation}

For a matched prefix, a known-rate estimate is undefined when every status in its
chosen family is unresolved; the lower and upper bounds remain defined.  Queries with no known usable anchors are
excluded from recall averages, and the evaluable rate is reported separately.  Macro
averages never impute missing query metrics.  Duplicate ranked memory identifiers,
missing assessments, negative runtime measurements, and non-finite values are
rejected by the reference implementation.

\begin{table}[ht]
  \centering
  \small
  \caption{Source-macro diagnostics within feasible matched-recall prefixes.
  These descriptive quantities do not use the infeasibility penalty.}
  \label{tab:matched-prefix}
  \input{generated/matched_prefix_diagnostics.tex}
\end{table}

\section{Additional Audited Examples}
\label{app:verification-examples}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{generated/verification_examples.pdf}
  \caption{Four abridged audited examples underlying the admissibility taxonomy:
  wrong principal, superseded current state, explicit forgetting, and a historical
  query that requires superseded states.  Packet IDs, memory IDs, released labels,
  and verdicts are preserved in the example manifest; wording is shortened for
  legibility.  These cases illustrate coverage, not population prevalence.}
  \label{fig:verification-examples}
\end{figure}

\section{Text-Only Inference Diagnostics}
\label{app:text-inference}

\begin{figure}[ht]
  \centering
  \includegraphics[width=\textwidth]{generated/inference_gap.pdf}
  \caption{Semantic verifiers recognize eligibility changes but do not apply them
  selectively.  (a) Feasibility and sign-reversed admissibility-risk differences
  versus namespace dense; labels give recall differences.  Whiskers are pointwise
  95\% intervals from 10,000 namespace-group bootstrap replicates stratified by
  source and query intent over 72 analysis cases.  (b) Controlled focal consistency
  versus stable-control overflip; labels report stable-admissible false-denial rate.
  The natural and controlled populations are separate and never pooled.}
  \label{fig:inference-gap}
\end{figure}

\begin{table}[H]
  \centering
  \small
  \caption{Candidate-level diagnostics for the natural text-only verification
  experiment.  Values use the fixed development-selected operating points.  Higher
  ROC-AUC, violation precision, and violation recall are better; lower required-anchor
  false-denial rate is better.}
  \label{tab:text-inference-diagnostics}
  \begin{tabular}{lrrrr}
    \toprule
    Model & ROC-AUC & Violation precision & Violation recall & Anchor false deny \\
    \midrule
    GPT-5.6 Sol & \OpenAIInferredRocAuc{} & \OpenAIInferredViolationPrecision{} &
      \OpenAIInferredViolationRecall{} & \OpenAIInferredAnchorFalseDeny{} \\
    Gemini 3.6 Flash & \GeminiInferredRocAuc{} & \GeminiInferredViolationPrecision{} &
      \GeminiInferredViolationRecall{} & \GeminiInferredAnchorFalseDeny{} \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Natural-Corpus Construction and Arms}
\label{app:arms}

RHELM development uses the first three persona-hash groups; evaluation uses the
remaining seven.  For each query, only conversation turns strictly earlier than its
date are candidates.  MemOps development uses the first 20\% of profile-hash groups;
evaluation uses the remaining 80 profiles.  Every method-visible dialogue memory in
the split is a global candidate.  MemOps has no cross-case wall clock, so its recency
arm uses released source order and is not a temporal-freshness claim.

All dense arms use \texttt{Qwen/Qwen3-Embedding-8B} at revision
\nolinkurl{1d8ad4ca9b3dd8059ad90a75d4983776a23d44af}, maximum length 512,
unit-normalized last-token pooling, float32 vectors, exact inner product, and stable
memory-ID tie breaking.  For unit vectors,
\[
s_{\mathrm{dense}}(m,q)=e_m^\top e_q.
\]
The hybrid arm uses reciprocal-rank fusion
\[
s_{\mathrm{RRF}}(m,q)=
\frac{1}{60+\operatorname{rank}_{\mathrm{BM25}}(m)}
+
\frac{1}{60+\operatorname{rank}_{\mathrm{dense}}(m)},
\]
and the recency control uses
$s_{\mathrm{rec}}(m,q)=s_{\mathrm{dense}}(m,q)+\gamma\rho(m)$, where
$\rho(m)\in[0,1]$ is normalized released order.

The threshold router incrementally assigns a memory to its most similar namespace
centroid when $\max_k\cos(e_m,c_k)\geq\theta$ and otherwise creates a cluster.  At
query time it selects at most $L$ centroids above $\theta$.  The cluster router uses
\[
\ell_k(x)=\gamma\log(n_k+\beta)+\frac{\cos(x,c_k)}{\tau},
\qquad
\ell_{\mathrm{new}}(x)=\log\alpha+
\frac{1-\max_j\cos(x,c_j)}{\tau_{\mathrm{new}}},
\]
assigns by the largest existing-or-new score, and routes over existing clusters.
An empty route in either router falls back to namespace-local dense retrieval.

\begin{table}[ht]
  \centering
  \small
  \caption{Frozen development-selected settings.  All arms use $k=100$.}
  \label{tab:selected-settings}
  \begin{tabular}{lp{0.68\linewidth}}
    \toprule
    Arm & Selected setting \\
    \midrule
    Global BM25 & $k_1=1.5,\ b=0.75$ \\
    Global dense & Parameter-free \\
    Global BM25+dense RRF & $k_1=0.9,\ b=0.75,\ k_{\mathrm{RRF}}=60$ \\
    Global recency dense & $\gamma=0.1$ \\
    Namespace dense & Parameter-free \\
    Query-agnostic current-only & Remove stale and superseded records for every query \\
    Historical released-field v1 & Retain every namespace-local record for history;
      for current-state queries, remove released policy-disallowed, stale, and
      superseded records \\
    Threshold router & $\theta=0.80,\ L=3$ \\
    Cluster router & $\alpha=1,\ \beta=1,\ \gamma=0.5,\ \tau=0.1,\
      \tau_{\mathrm{new}}=0.4,\ L=1$ \\
    \bottomrule
  \end{tabular}
\end{table}

The corrected post-hoc \texttt{released\_governance\_oracle\_v2} is not one of the
nine development-selected arms.  It applies query-memory policy decisions to both
history and current-state queries, then applies lifecycle filtering only to
current-state intent.  All v2 attribution settings are fixed at top-100 and receive
no tuning.

\begin{table}[ht]
  \centering
  \small
  \caption{All nine frozen v1 arms on the source-macro public evaluation.  Penalized
  non-usable upper risk assigns 1.0 to infeasible or unresolved query outcomes.
  Candidates scored is a local diagnostic, not a production latency estimate.}
  \label{tab:all-arms}
  \input{generated/full_arm_results.tex}
\end{table}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{generated/retrieval_results.pdf}
  \caption{Frozen v1 retrieval diagnostics.  (a) Recall versus mean candidates
  scored for all nine arms.  (b) Namespace-dense minus global-dense effects under
  the historical non-usable risk.  (c) Conditional non-usable bounds and label
  coverage.  (d) Threshold, cluster, and historical released-field v1 differences
  versus namespace dense.  This figure preserves the original route semantics and
  cannot be read as the corrected v2 policy/lifecycle attribution.}
  \label{fig:retrieval}
\end{figure*}

\section{GateMem Routing, Scoring, and Prediction}
\label{app:gatemem}

G0 is the exact upstream \texttt{rag\_naive} global TF--IDF route and G1 is the
upstream \texttt{rag\_policy} route with released policy logic.  G2 starts at the
authenticated asker, forms a connected component from explicit relationship IDs,
and admits chunks spoken by principals in that component; it reads neither query or
memory text nor leak targets when deciding support.  G3 adds only explicit pre-query
delete or supersede operations.  Because the release contains none, G3 is
trace-identical to G2.  G0 and G1 are upstream methods; G2 and G3 are diagnostics.

Each reader prompt contains the question and ordered retrieved record IDs and texts,
but not the arm name, scores, target strings, expected action, or gold answer.  Both
readers use temperature zero, JSON mode, and at most 384 output tokens.  The runs are
fresh, with no judge or output repair.  A strict parser accepts one object containing
action, answer, structured answer, and a string-list audit field; the audit field is
not scored, and parse failures receive conservative outcomes.

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{generated/evidence_summary.pdf}
  \caption{Observational GateMem evidence.  (a) Exposed-minus-unexposed answer
  disclosure over \ReaderDiscordantCount{} discordant checkpoints.  (b) Predictive
  value of exposure on \ReaderPredictionCount{} held-out rows.  (c) Reader A G1-minus-G0
  leakage, bounded utility, and over-refusal.  Readers are not pooled; route contrasts
  are non-causal and leakage and utility use different populations.}
  \label{fig:gatemem-observational}
\end{figure}

Privacy checkpoints use the released privacy context-leak and answer-leak rules;
safety checkpoints use the corresponding deletion rules.  The disclosure outcome
is the released rule-based end-to-end target indicator.  Utility correctness and
over-refusal are bounded rule-based auxiliary outcomes rather than LLM-judge scores.
Parse failures are handled conservatively as leakage for privacy or safety and as
incorrect plus over-refusal for utility.

The incremental predictor is logistic regression with $C=1$, the \texttt{lbfgs}
solver, and at most 2,000 iterations.  Categorical features are arm, domain, and
query type; additional baseline features are route non-emptiness and standardized
route width.  The augmented model adds only target exposure.  Each held-out episode
is scored by a model fit on all other episodes.  Brier and log-loss differences are
then resampled over episodes with 10,000 deterministic bootstrap replicates.

\section{Controlled Exposure Construction}
\label{app:paired-construction}

Each of the 16 scenarios has two paraphrased query pairs, allow and block conditions,
and three manipulated roles: a relevant focal candidate, an irrelevant-admissible
candidate, and an irrelevant-inadmissible candidate.  The focal candidate is
admissible under allow and inadmissible under block.  For every
query--condition--candidate unit, two requests differ only in whether that candidate
is exposed.  This yields 192 paired units and 384 requests for each reader.

Every candidate carries a unique literal marker absent from the query and all other
candidates.  The normalized-substring scorer counts the marker even inside a refusal;
appearance after withholding is hallucinated disclosure.  Requests are stateless and
deterministically interleaved.  The original GPT, Gemini, and DeepSeek readers and the
separately executed Claude Opus 5 replication each complete all 384 requests with one
attempt per request.  There is no transport retry, judge, output repair, selective
rerun, or model pooling.  The released result packages contain only content-free pair
scores, aggregates, hashes, costs, and bootstrap intervals.

\section{Mechanism Smoke}
\label{app:smoke}

The candidate-pool smoke uses four development packets and 16 evaluation packets,
with 7--12 candidate records per packet.  Namespace support preserves observed
recall at \SmokeGlobalRecall{} (global) and \SmokeNamespaceRecall{} (namespace),
  changes measured wrong-scope leakage from \SmokeGlobalWrongScope{} to
  \SmokeNamespaceWrongScope{}, and changes the measured non-usable rate from
  \SmokeGlobalNonUsableRate{} to \SmokeNamespaceNonUsableRate{}.  This verifies
metric behavior under a known scope intervention; it is not a natural-corpus
performance estimate.

\begin{table}[ht]
\centering
\caption{Candidate-pool mechanism smoke on 16 curated evaluation packets.}
\label{tab:smoke}
\input{generated/mechanism_smoke.tex}
\end{table}

\section{Reproducibility and Provenance}

All paper numbers are generated from the normalized evidence families enumerated in
\texttt{evidence/README.md}.  Each CSV has a manifest recording its output hash,
frozen source paths and hashes,
source-snapshot identity, and transformation-script hash.  The natural execution
manifest proves complete coverage of nine settings over 3,767 queries in 33,903
route rows and 33,903 score rows.  Tests bind every estimate and confidence interval
back to the machine-readable claim contract.  The public package includes controlled
prompt templates, synthetic candidate definitions, and content-free derived scores;
it regenerates every reported table and figure without a provider call.  Instantiated
natural benchmark payloads, exact provider request/response bundles, and normalized
private provider outputs are not distributed, so the package cannot independently
audit the provider-output-to-score boundary or reproduce the original executions.

\input{checklist.tex}

\end{document}
